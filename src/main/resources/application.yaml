spring:
  application:
    name: chupapo-api

  datasource:
    url: jdbc:postgresql://localhost:5432/chupapo
    username: chupapo
    password: chupapo

  jpa:
    hibernate:
      ddl-auto: create-drop