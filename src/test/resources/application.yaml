spring:
  datasource:
    url: jdbc:h2:mem:chupapo-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop

app:
  bootstrap-admin:
    password:
